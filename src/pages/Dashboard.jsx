import React from 'react';
import HeroSection from './Herosection';

export default function Dashboard() {
  return (
    <div>
      {/* For now reuse HeroSection as requested */}
      <HeroSection />
    </div>
  );
}
